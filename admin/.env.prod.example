NODE_ENV=production
PORT=3002

# Base de datos y cache (red interna Docker)
MONGO_URL=mongodb://mongo:27017
DB_NAME=sgsma2026
REDIS_URL=redis://redis:6379

# MQTT (red interna Docker)

# Auth
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# URL pública del backoffice (para links de uploads: QR, imágenes de anuncios, etc.)
PUBLIC_BASE_URL=https://sgsma2026.cl/backoffice

# URL interna del API Fastify (para envío de emails de recuperación)
FASTIFY_API_URL=http://sgsma26_apimobile:3001

# Gmail para notificaciones (opcional)
GMAIL_USER=
GMAIL_APP_PASSWORD=
